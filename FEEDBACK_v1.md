# 第一版问题反馈 —— DIY旅游定制助手

**反馈日期：2026-05-15**
**反馈对象：Claude Code**
**参考文档：PRD_v6.md | TASK_BREAKDOWN.md**

---

## 问题 1：卡片页面为空，无法进行偏好评分

**现象：** 打开卡片页面后，没有 POI 卡片展示，无法左滑右滑打分。

**关联文档：**
- PRD_v6.md §3.2 卡片流偏好收集
- PRD_v6.md §6.2 API `/api/v1/poi/cards`
- TASK_BREAKDOWN.md T4 验收标准

**排查方向：**

1. **API 是否返回数据？**
   ```bash
   curl http://localhost:8000/api/v1/poi/cards?session_id=xxx
   ```
   应返回 3 张卡片 JSON。如果返回空数组 `[]`，说明 poi_cache.db 没数据或查询逻辑有误。

2. **前端是否正确请求了 API？**
   - 检查 `api/client.ts` 中的 `/api/v1/poi/cards` 请求
   - 检查 session_id 是否已创建（应先调 `/api/v1/session`）

3. **卡片组件是否正确渲染？**
   - `PoiCard.tsx` 是否正确接收了 API 返回的 JSON？
   - `CardDeck.tsx` 是否渲染了 3 张卡片？
   - react-tinder-card 是否正确初始化？

**预期行为（参考 PRD §3.2.1）：**

打开卡片页面后，用户应看到 3 张可滑动的 POI 卡片，每张卡片含：
- 封面区（目前 MLP 阶段用纯色 + POI 名称水印）
- POI 名称 + 高德评分 + 类型
- 摘要（目前用高德 tags 拼接，如"骑行 · 日出 · 拍照"）
- 时长和费用信息
- 底部手势提示（👈 不感兴趣 / 想去！ 👉）

---

## 问题 2：自然语言对话过于苍白，未体现 LLM 的作用

**现象：** 约束收集阶段的对话生硬，像固定问答列表，看不出 LLM 在做理解和推理。

**关联文档：**
- PRD_v6.md §3.3 约束收集（含智能推断规则表）
- PRD_v6.md §7.1 Agent 架构（含 System Prompt 模板）
- PRD_v6.md §10.2 Agent 自主决策范围
- TASK_BREAKDOWN.md T5 验收标准

**具体问题与改进：**

### 问题 2.1：对话缺少智能推断

当前对话可能类似：
```
Agent: "出行人数？"
User: "2"
Agent: "到达时间？"
User: "5月1日"
```

这像是表单填写，不是 Agent 对话。

**期望行为（参考 PRD §3.3 智能推断规则）：**

```
User: "我和女朋友去大理玩"
Agent: "两个人一起，听起来不错~ 你们计划几号到？住哪个片区呀？"
```
→ Agent 从"女朋友"推断人数=2，跳过人数问题，追问时间和住宿。

```
User: "带爸妈去"
Agent: "带长辈的话，我会把节奏安排得轻松些。你们几个人？几号到大理？"
```
→ Agent 推断节奏=休闲型，同时温情回应。

### 问题 2.2：没有"自然语言 → 结构化约束"的转换

**期望行为：**

用户输入"住古城南门附近" →
- Agent 调用 `amap_geocode("大理古城南门")` → 拿到经纬度
- 存入 UserPreference: `accommodation_lat=25.6xxx, accommodation_lon=100.1xxx`
- 回复："南门附近对吧？我记住了~"

用户输入"随便/都行" →
- Agent 使用默认值
- 回复："那我就按休闲节奏帮你排~"

### 问题 2.3：没有冲突检测与提醒

**期望行为：**

用户标记了 12 个"想去"但只玩 3 天 →
Agent 应主动提醒："12 个去处 3 天有点赶，我会按评分帮你挑 6~8 个最合适的~"

### 问题 2.4：System Prompt 未体现个性化

参考 PRD §7.1 System Prompt 模板：

```yaml
role: 旅行规划师
identity: 友好、专业、有主见的旅行助手
behavior_rules:
  - 对话风格：像朋友推荐而非客服回答，用"~"结尾增加亲和力
  - 智能推断：理解模糊表述，推断隐含约束，但需确认
  - 纠错能力：用户说"不对"时立即调整
```

当前对话如果像"客服机器人"，说明 System Prompt 没有正确配置。

---

## 问题 3：规划结果包含大量房地产/住宅小区，而不是旅游景点

**现象：** 最终行程中出现了小区名称（如"XX花园"、"XX新城"），这些不是旅游目的地。

**关联文档：**
- PRD_v6.md §9.3 POI 筛选标准
- PRD_v6.md §4 数据管道——离线数据建设
- TASK_BREAKDOWN.md T1（高德 POI 采集）

**根因分析：**

高德 POI 采集时，搜索范围过宽或 POI 类型筛选不正确，把"住宅小区"类 POI 也收录了。

**修复要求：**

### 3.1：高德 POI 类型过滤

采集脚本 `pipeline_amap_fetch.py` 中，高德 API 请求应明确指定 POI 类型，排除非旅游类：

```
✅ 应保留的类型：
  - 风景名胜 (110000)
  - 公园广场 (110100)
  - 纪念馆 (080300)
  - 寺庙道观 (080400)
  - 教堂 (080500)
  - 国家级景点 (110200)
  - 省级景点 (110300)
  - 餐饮（美食类 POI）
  - 购物（特色街区/市场）

❌ 应排除的类型：
  - 住宅小区 (120300)
  - 商务住宅 (120200)
  - 公司企业 (170000)
  - 政府机关 (130000)
  - 学校 (141200)
  - 医院 (090000)
```

### 3.2：补充 POI 名称关键词过滤

即使类型正确，POI 名称含以下关键词也应排除：
- "花园"、"新城"、"花苑"、"苑"（住宅小区常见后缀，但"大理古城"不在此列）
- "公司"、"集团"、"办事处"
- "门诊"、"医院"、"诊所"

### 3.3：验收标准

采集完成后运行 `check_data.py`，应输出：

```
POI 采集完成: 87 条
  景点类: 42
  美食类: 28
  购物类: 12
  活动类: 5
  疑似非旅游类: 0  ← 此行必须为 0
```

参考 PRD §9.3 筛选标准：高德评分 ≥3.8、评价数 ≥100、有明确营业时间、游客友好。

---

## 问题 4：规划结果没有生成路径地图

**现象：** 行程展示页面只有文字列表，没有在地图上可视化路线。

**关联文档：**
- PRD_v6.md §3.5 结果展示与调整（时间线视图 + 地图）
- PRD_v6.md §6.1 技术选型
- TASK_BREAKDOWN.md T7（行程展示）

**修复要求：**

### 4.1：在行程页面嵌入高德地图

T7 的 `Result.tsx` 页面需新增地图组件：

```
┌─────────────────────────────────────────┐
│ 📅 第 1 天                               │
├──────────────────────┬──────────────────┤
│ 时间线列表            │   [地图]          │
│                      │                  │
│ 09:00 大理古城        │   📍 1 古城       │
│ 11:30 再回首米线      │   ┃              │
│ 13:00 寂照庵         │   📍 2 米线       │
│ 17:00 龙龛码头        │   ┃              │
│                      │   📍 3 寂照庵     │
│                      │   ┃              │
│                      │   📍 4 龙龛码头   │
│                      │                  │
├──────────────────────┴──────────────────┤
│ 切换：第 1 天 | 第 2 天 | 第 3 天         │
└─────────────────────────────────────────┘
```

### 4.2：地图功能规格

- **地图 SDK：** 高德 JS API 2.0（前端加载）
- **标注点：** 每个 POI 用带序号的 Marker 标注（📍1, 📍2, ...）
- **连线：** 按路线顺序用 Polyline 连接（不同天用不同颜色）
- **住宿点：** 用特殊 Marker 标记（🏠）
- **缩放：** 自动 fitView，包含当天所有 POI
- **交互：** 点击 Marker 弹出 POI 基本信息卡片

### 4.3：实现方式

```
frontend/src/components/
├── RouteMap.tsx           ← 新增：地图组件
│   引入高德 JS API
│   接收 dailyRoute.poi_sequence 作为输入
│   渲染 Markers + Polyline
```

在 `TimelineDay.tsx` 中，时间线列表和 `RouteMap` 左右并排（PC 端 60/40 分栏）。

### 4.4：相关 API 数据

后端 `/api/v1/planner/run` 返回的每个 POI 已包含 `lat` 和 `lon`，前端地图直接使用，无需额外接口。

---

## 总结

| # | 问题 | 严重程度 | 关联任务 | 修复优先级 |
|---|------|---------|---------|-----------|
| 1 | 卡片页面为空 | 🔴 阻塞 | T4 | P0 |
| 2 | LLM 对话苍白 | 🟡 体验差 | T5 | P1 |
| 3 | 房地产混入景点 | 🔴 数据脏 | T1 | P0 |
| 4 | 缺少路径地图 | 🟡 功能缺 | T7 | P1 |

> **给 Claude Code 的指令：请逐一修复以上 4 个问题，每修复一项后暂停让我验证，不要一次性全改完。**
