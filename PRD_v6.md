# DIY旅游定制助手——产品需求文档

**版本：v6.0 | 日期：2026-05-14 | 状态：待开发**
**性质：独立完整版 | 基于 v5 + prd-checklist 评审 + prd-review-panel 评审打磨**

---

## 目录

1. [产品概述](#1-产品概述)
2. [总体架构与数据流](#2-总体架构与数据流)
3. [核心功能流程](#3-核心功能流程)
4. [数据管道——离线数据建设](#4-数据管道离线数据建设)
5. [数据模型](#5-数据模型)
6. [前端设计](#6-前端设计)
7. [后端 Agent 设计](#7-后端-agent-设计)
8. [非功能性需求](#8-非功能性需求)
9. [POI 知识库构建策略](#9-poi-知识库构建策略)
10. [Agent 行为规约与边界](#10-agent-行为规约与边界)
11. [评估方案与质量保障](#11-评估方案与质量保障)
12. [上线策略与演进路线](#12-上线策略与演进路线)
13. [风险与应对](#13-风险与应对)
14. [开发路线图](#14-开发路线图)
15. [关键决策记录](#15-关键决策记录)
16. [技术选型总表](#16-技术选型总表)

---

## 1 产品概述

### 1.1 背景与痛点

自由行用户做攻略时面临四大痛点：

| 痛点 | 现状 | 本产品如何解决 |
|------|------|---------------|
| 信息过载 | 需跨小红书/马蜂窝/携程筛选碎片化攻略，平均耗时 4~6 小时 | 聚合多平台真实评价，一张卡片展示，10~15 分钟完成偏好收集 |
| 路线难排 | 景点是否顺路、时间是否冲突，全靠人工判断和反复查地图 | 规划引擎自动求解带时间窗的路线问题，一键获得最优路线 |
| 偏好丢失 | 攻略作者的口味不等于用户偏好，跟着走常常失望 | 用户刷卡打分，系统学习个人偏好，千人千面 |
| 动态调整难 | 现场关门/下雨/累了，临时改计划手忙脚乱 | 支持删除/新增/重排，一键重规划，10 秒内出新方案 |

#### 用户故事

> _小王和女朋友计划五一去大理玩 3 天。打开小红书看了 2 小时攻略，收藏了 23 个笔记，但完全不知道怎么串成路线。哪些景点离得近？中午在哪吃饭？第二天会不会太赶？最后花了 4 个小时手动排了一个版本，到了大理发现苍山索道排队 2 小时，全乱了。_

**即使用户不是「懒」，传统攻略工具也解决不了路线规划的结构性问题。**

### 1.2 为什么用 AI Agent 而不是传统方案

| 对比维度 | 传统行程工具（马蜂窝/穷游行程助手） | 传统算法推荐（基于标签匹配） | **本产品：AI Agent** |
|---------|----------------------------------|---------------------------|---------------------|
| 输入方式 | 用户手动勾选景点 + 拖拽排序 | 输入偏好标签，算法推荐 | **对话式交互 + 刷卡自然表达偏好** |
| 规划能力 | 无自动优化，用户自行判断 | 固定规则排序，无路线优化 | **贪心+2-opt 求解，真正优化通勤成本** |
| 适应性 | 静态行程，无法动态调整 | 无法处理临时变更 | **支持删除/新增/重排，秒级重规划** |
| 信息质量 | 依赖单一平台 UGC，质量参差 | 无评价聚合 | **Bing 搜索聚合多平台真实评价+LLM 提炼** |
| 个性化 | 无 | 基于标签的粗粒度匹配 | **刷卡评分+多轮对话，细粒度偏好建模** |
| 冷启动 | 用户自行研究 | 热门推荐 | **冷启动热度分+随机扰动，探索与利用平衡** |

**Agent 方案的核心价值**：不是「生成一份攻略」而是「模拟一个懂你的旅行规划师」。它能理解你说的「不要太累」是什么意思，能在你改变主意时立刻调整，能告诉你「放弃苍山是因为时间不够，建议换成寂照庵，不用排队」。

### 1.3 产品定位

**对话式 AI Agent 驱动的 DIY 旅游规划工具。**

三段式体验，每段都有明确开始/结束状态：

```
[输入目的地] → [卡片流偏好收集] → [多轮约束对话] → [秒级路径规划] → [交互式行程调整]
     │                │                  │                 │                   │
  城市确认       10~30 张卡片        5~8 个问题       贪心+2-opt          拖拽/增删
```

1. **探索刷卡**（3~8 分钟）— 像刷 Tinder 一样浏览 POI 卡片，左右滑动打分表达偏好。系统学习你喜欢的 POI 类型、节奏、风格。
2. **偏好学习**（2~5 分钟）— 多轮对话收集软硬约束（出行人数、时间、节奏、预算等）。Agent 会追问模糊表述，智能推断隐含信息。
3. **智能排程**（≤10 秒）— 贪心+2-opt 求解器输出每日行程方案。展示完整路线、时间分配、费用预估、放弃原因。

#### Agent 角色身份

本产品的 Agent 在用户感知层面是一个**友好、专业、有主见的旅行规划师**：
- **友好**：用「洱海骑行真的很舒服，建议早点去避开人流」而非「洱海生态廊道评分 4.5」
- **专业**：能解释路线逻辑「因为苍山和寂照庵在同一条线，所以安排在第二天上午」
- **有主见**：敢于推荐替代方案「苍山排队太久，寂照庵的多肉花园更出片，而且免费」

### 1.4 核心差异点

| 差异点 | 竞品现状 | 本产品方案 |
|--------|---------|-----------|
| **评价质量** | 单一平台 UGC，信息碎片化、真假难辨 | Bing 搜索聚合多平台真实评论 + LLM 提炼 40 字摘要，标注来源平台 |
| **图片真实感** | 官方精修图或无人像风景 | Bing 图片搜索采集游客实拍 + 高清风光，封面+画廊双层展示 |
| **响应速度** | 在线搜索/计算，每次操作等待数秒 | 全部离线预计算，用户端毫秒级响应 |
| **个性化深度** | 基于标签/历史浏览 | 刷卡手势评分 + 多轮对话约束 + 行为反馈闭环 |
| **交互形式** | 表单/列表/地图标记 | Tinder 式卡片滑动，直觉化偏好表达 |

### 1.5 用户画像

| Persona | 场景 | 核心需求 |
|---------|------|---------|
| **大学生小王** | 预算有限，第一次自由行，想去大理 3 天 | 怕踩坑、怕超预算、要出片、路线别太赶 |
| **上班族李姐** | 年假 5 天，带父母去成都，要舒适不要特种兵 | 节奏慢、少走路、有午休时间、餐厅要靠谱 |
| **情侣档阿杰** | 和女朋友度周末，杭州 2 天，要浪漫要美食 | 出片景点、网红餐厅、避开人流高峰 |

### 1.6 成功指标与采集方式

| 指标 | v1 目标 | 数据采集方式 |
|------|---------|-------------|
| 偏好收集完成率 | ≥80% | 后端记录「输入目的地 → 点击开始排程」完整流程的比例 |
| 首次排程满意度 | ≥70% | 用户未在 5 分钟内触发「重新规划」的比例（隐式） |
| 平均规划耗时 | ≤25 分钟 | 后端记录 Session 从开始到获得首版行程的 wall time |
| 路径规划响应 | ≤10 秒 | 后端 run_planner 函数耗时打点 |
| 推荐准确率 | ≥60% | 右滑率（右滑次数 / 总评分次数，排除下滑跳过） |

---

## 2 总体架构与数据流

### 2.1 完整时序——离线 vs 在线

**核心设计原则：耗时操作全部离线完成，用户使用时全部毫秒级响应。**

```
━━━━━━━━━━ 离线阶段（开发者运行，用户无感知）━━━━━━━━━━

Phase 1: 高德 POI 采集           ~10 分钟    → 基础信息入库
Phase 2: Bing 搜索管道（摘要）    ~7 分钟     → 40 字 LLM 摘要
Phase 3: Bing 图片管道（图片）    ~5 分钟     → 封面+画廊
Phase 4: 高德离线距离矩阵         ~53 分钟    → 全 POI 对距离（可后台）
Phase 5: 质量抽检                ~5 分钟     → 抽 10 个 POI 人工复核

总计 Ph 1~3：约 22 分钟/城市
Ph 4 可后台跑，不阻塞。Ph 1~3 阻塞后续开发。
输出：poi_cache.db + images/{city_id}/ → 部署到服务器
      距离矩阵 SQLite 独立文件

缓存策略：摘要+图片+距离矩阵 TTL=30 天。
超过 TTL 后触发全量刷新（非增量，因无法追踪源头变更）。
刷新期间不影响线上服务（用旧缓存，刷新完成后原子替换）。

━━━━━━━━━━ 在线阶段（用户使用）━━━━━━━━━━

用户操作                      系统响应         耗时
─────────────────────────────────────────────────
打开页面                     首屏渲染          < 2 秒
输入「大理 3 天」             查城市 + POI 库    < 1 秒
卡片流（刷卡评分）            读本地缓存         < 100 ms
上滑看详情（含全画廊）        查 POI 详情        < 200 ms
约束收集对话                  LLM 推理          1~3 秒/轮
路径规划                      算法 + 查距离矩阵   < 10 秒
行程调整（增删改）            增量重规划          < 5 秒
行程展示切换                  查已生成数据       < 50 ms

⚡ 用户从未等待超过 10 秒。最长等待 = 路径规划首版。
```

### 2.2 In Scope / Out of Scope

#### ✅ In Scope（v1 必做）

- 单城市旅游规划（大理 MVP）
- PC 端 Web 应用
- 5 种手势的卡片滑动交互
- 基于贪心+2-opt 的单日/多日路径规划
- 多轮对话约束收集（LLM 驱动）
- 离线 Bing 搜索摘要管道
- 离线 Bing 图片管道
- 离线高德距离矩阵
- 行程结果展示（每日时间线视图）
- 行程调整（增删 POI、改节奏 → 重规划）
- 基础错误处理（城市不在库、POI 无数据、规划无解）

#### ❌ Out of Scope（v1 故意不做）

- 多城市/跨城规划（含城际交通）
- 移动端 App（仅 PC Web）
- 实时交通路况（使用离线距离矩阵，不调用实时 API）
- 酒店/机票/门票预订（纯规划工具，不涉及交易）
- 社交功能（分享、评论、点赞）
- 多语言支持（仅中文）
- 付费/订阅（纯个人学习项目，不商用）
- 多人协作规划
- 天气集成（v1 不集成天气 API，靠用户常识）

### 2.3 部署架构

```
┌──────────────────────────────────────────┐
│ 前端 — React SPA（Vite + TypeScript）      │
│                                          │
│ ┌──────────┐ ┌────────┐ ┌─────────────┐  │
│ │TinderCard│ │约束表单│ │时间线行程面板 │  │
│ │(滑动卡片) │ │(对话式) │ │(swiper画廊)  │  │
│ └──────────┘ └────────┘ └─────────────┘  │
│                                          │
│ 状态管理: zustand (轻量, < 2KB)           │
│ 路由: react-router v6                     │
└──────────────┬───────────────────────────┘
               │ REST API (JSON) + JWT Auth
┌──────────────▼───────────────────────────┐
│ 后端 — FastAPI (Python 3.12)              │
│                                          │
│ ┌──────────────────────────────────────┐ │
│ │ 路由层                                │ │
│ │ /api/city  /api/poi  /api/planner    │ │
│ │ /api/profile  /api/session           │ │
│ └──────────────────────────────────────┘ │
│ ┌──────────────────────────────────────┐ │
│ │ Agent 层：LangChain ReAct             │ │
│ │ 状态机 + Prompt 管理 + 工具调用编排    │ │
│ └──────────────────────────────────────┘ │
│ ┌──────────────────────────────────────┐ │
│ │ 工具层                                │ │
│ │ 资源获取 | 用户状态 | 规划引擎 | 地图  │ │
│ └──────────────────────────────────────┘ │
└──────────────┬───────────────────────────┘
               │
┌──────────────▼───────────────────────────┐
│ 数据层（均为本地 SQLite）                  │
│ poi_cache.db      — POI + 摘要 + 图片路径 │
│ distance.db       — 离线距离矩阵           │
│ user.db           — 用户 + Session + 评分  │
└──────────────────────────────────────────┘
```

---

## 3 核心功能流程

### 3.1 阶段一：目的地输入

#### 正常流程

```
用户输入："我要去大理玩 3 天"
  ↓
Agent 解析：目的地="大理"，游玩天数=3
  ↓
查询 poi_cache.db：大理 POI 共 87 条，覆盖 4 个片区
  ↓
展示城市卡片（名称、推荐天数、最佳季节、片区简介）
  ↓
"大理有 87 个去处，准备好了吗？开始刷卡~"
  ↓
进入卡片流
```

#### 异常流程

| 异常 | Agent 响应 |
|------|-----------|
| 城市名拼写错误 | "没找到「大利」的相关信息，你是想说「大理」吗？"（模糊匹配 + 确认） |
| 城市不在库内 | "抱歉，目前只支持大理（即将支持成都、杭州）。你可以选大理体验一下吗？" |
| 天数不合理（>14 或 <1） | "大理玩 20 天可能太久了哦，一般 2~4 天比较合适。你确定吗？" |
| 输入歧义 | "「云南」是一个省，你说的是大理、丽江还是昆明？" |

### 3.2 阶段二：卡片流偏好收集

#### 3.2.1 卡片完整设计（含所有状态）

```
┌──────────────────────────────────┐
│          ← 屏幕宽度 →             │
│         「已看 3/30」             │  ← 进度指示
│                                  │
│    ┌────────────────────────┐    │
│    │                        │    │
│    │   [封面美图 ~50KB]      │    │  ← 加载中: skeleton 灰色块
│    │   ← swiper 左右滑 →     │    │     加载失败: 默认占位图 + "图片获取中"
│    │   共 5 张图             │    │
│    │                        │    │
│    ├────────────────────────┤    │
│    │  洱海生态廊道            │    │
│    │  ⭐ 4.5 | 景点 | 1280人评│    │
│    │                        │    │
│    │  "环湖骑行很舒服，建议    │    │  ← 40 字 LLM 摘要
│    │   早上 8 点去避开人流。   │    │
│    │   龙龛码头最出片。"      │    │
│    │  💬 综合 127 条真实评价  │    │  ← 来源标注
│    │     (什么值得买/知乎/携程)│    │
│    │                        │    │
│    │  🕐 2~3h  💰 ¥50~100   │    │
│    │  📍 古城片区             │    │
│    └────────────────────────┘    │
│                                  │
│   👈 不感兴趣      想去！ 👉     │
│    (红色飞出)      (绿色飞出)    │
└──────────────────────────────────┘
```

#### 3.2.2 手势映射与动画

| 手势 | 含义 | 自动打分 | 视觉反馈 |
|------|------|---------|---------|
| 右滑 | 想去 | 5⭐ | 卡片绿色边框 + 向右飞出 + 🟢"想去"标签 |
| 左滑 | 不感兴趣 | 1⭐ | 卡片红色边框 + 向左飞出 + 🔴"跳过"标签 |
| 上滑 | 查看详情 | 不变 | 卡片展开为详情面板（全画廊、详细评价） |
| 下滑 | 跳过这张 | 不评分 | 卡片淡出，不扣分不加分 |
| 点击⭐ | 精确打分 | 1~5⭐ | 星级高亮动画 + 卡片缩小飞出 |

已评分的卡片可通过底部「已评分列表」回看和修改评分。

#### 3.2.3 推荐策略与排序算法

```
Score(poi) = 热度分 × 0.30          (基于 Bing 搜索提及频次)
           + 类型多样性 × 0.20       (与已展示类型的余弦距离)
           + 用户偏好相似度 × 0.25   (与已评分 POI 的特征相似度)
           + 地理分散度 × 0.15       (与已评分 POI 的平均距离)
           + 冷启动随机扰动 × 0.10   (-0.05 ~ +0.05 随机噪声)

推荐策略细节：
- 冷启动（用户0评分）：按热度分+类型多样性排序，确保前 10 张覆盖 4 种类型
- 用户已评分 ≥3 个后：偏好相似度权重从 0 增至 0.25
- 每次展示 3 张卡片（同时渲染），用户评分后立即补充新卡
- 已展示的 POI 不出现在后续卡片中（除非用户主动回看）
- 同片区 POI 间隔至少 2 张卡片展示（避免「全是古城」的视觉疲劳）
```

#### 3.2.4 退出条件

| 退出方式 | 触发条件 | Agent 响应 |
|---------|---------|-----------|
| 主动结束 | 用户点击「开始排程」| "好嘞，你一共标记了 8 个想去的。来聊聊具体安排吧~" |
| 评分饱和 | 已评分 ≥ 15 个 | "你已经评价了不少了，够我帮你排了。要继续刷还是开始排？" |
| 展示饱和 | 已展示 ≥ 30 张 | "87 个去处你已浏览了 30 个，要先开始排吗？" |
| 用户催促 | 输入"够了/直接排吧/不刷了" | 立即跳转到约束收集阶段 |

#### 3.2.5 Session 恢复

用户关闭浏览器后重新打开 → 后端根据 Session ID 恢复：
- 已评分列表 + 评分值
- 已展示但未评分的 POI 列表
- 当前阶段（卡片流 / 约束收集 / 规划结果）
- 恢复后从断点继续，不重来

### 3.3 阶段三：约束收集

以对话形式逐步收集约束。Agent 主动提问，用户自由回答，Agent 解析并确认。

| 约束项 | 必填 | 默认值 | Agent 提问示例 |
|-------|------|--------|---------------|
| 出行人数 | ✅ | — | "几个人一起去呀？" |
| 到达/离开时间 | ✅ | — | "第一天几点到大理？最后一天几点走？" |
| 住宿位置 | 可选 | 市中心 | "住哪个片区？（古城/双廊/喜洲/海东）" |
| 交通方式 | 可选 | 公交+步行 | "在大理怎么出行？（打车/租车/公交/骑车）" |
| 游玩节奏 | 可选 | 休闲型 | "喜欢紧凑玩满一天，还是悠闲慢慢逛？" |
| 美食偏好 | 可选 | 综合型 | "对吃有什么偏好？（本地小吃/网红打卡/高档餐厅）" |
| 预算 | 可选 | 不限 | "大概多少人均预算？（不含住宿和机票）" |
| 特殊需求 | 可选 | 无 | "有什么特别要求？（比如不能爬山/要拍很多照片）" |

#### 智能推断规则

| 用户表述 | Agent 推断 | 需确认 |
|---------|-----------|--------|
| 「我和女朋友」 | 人数 = 2 | 「你们两个人一起去对吗？」 |
| 「带爸妈」 | 节奏 = 休闲型，人数 ≥ 3 | 「带长辈的话，节奏我会安排得轻松些哦」 |
| 「住古城南门附近」 | 调用高德 API 解析经纬度 | 「南门附近对吧？我记住了」 |
| 「随便/都行」 | 使用默认值 | 「那我就按休闲节奏帮你排~」 |
| 「不要太赶」 | 节奏 = 休闲型 | 「好的，每天安排轻松些」 |

#### 约束冲突处理

| 冲突 | 处理方式 |
|------|---------|
| 3 天但标记了 12 个想去 | "12 个去处 3 天有点赶，我会按评分帮你挑 6~8 个最合适的" |
| 休闲节奏但景点间距离远 | "环洱海一圈挺远的，休闲节奏的话建议只逛一半" |
| 预算低但包含高消费 POI | "寂照庵免费又出片，比 ¥200 的网红餐厅更值哦" |

### 3.4 阶段四：路径规划引擎

#### 3.4.1 问题形式化定义

**输入变量：**
- $P = \{p_1, \dots, p_n\}$ — POI 集合，每个 $p_i$ 含 $(lat, lon, duration, [open, close], score_i)$
- $D = [d_{ij}]_{n \times n}$ — 离线距离矩阵，$d_{ij}$ 为 POI $i$ 到 $j$ 的驾车/公交时间
- $K$ — 游玩天数
- $T_{day}$ — 日容量（分钟）
- $T_{lunch}, T_{dinner}$ — 用餐时间窗

**决策变量：**
- $x_{ijk} \in \{0,1\}$ — 第 $k$ 天 POI $i$ 之后访问 $j$
- $a_{ik}$ — 第 $k$ 天到达 POI $i$ 的时间

**目标函数：**

$$\max \sum_{k=1}^K \sum_{i=1}^n \left( score_i \cdot \mathbf{1}[p_i \in route_k] \right) - \lambda \sum_{k} \sum_{i,j} d_{ij} \cdot x_{ijk}$$

其中 $\lambda = 0.01$ 为通勤惩罚系数。

**约束条件：**
- 时间窗：$open_i \leq a_{ik} \leq close_i - duration_i$
- 日容量：$\sum_{i \in route_k} (duration_i + \sum_j d_{ij}x_{ijk}) \leq T_{day}$
- 午餐窗：若当日有美食 POI，安排在 $[11:30, 13:00]$
- 晚餐窗：$[17:30, 19:00]$
- 交通衔接：$a_{jk} \geq a_{ik} + duration_i + d_{ij}$

#### 3.4.2 三层求解算法

```
Layer 1: 天级分配（贪心）
  - 按 score 降序排列所有 POI
  - 依次尝试将 POI 塞入剩余容量最多的天
  - 跨天交换优化（尝试将高评分 POI 从满的天移到不满的天）

Layer 2: 日内排序（插入启发式 + 2-opt）
  - 起始 POI = 距离住宿最近的
  - 每次选择「剩余 POI 中插入后增加时间最少」的 POI
  - 插入时检查时间窗（营业时间、午餐/晚餐窗）
  - 查离线距离矩阵获取精确通勤时间
  - 2-opt 局部优化 100 轮（随机交换两个 POI 位置，若更优则保留）

Layer 3: 可行性检查
  - 若某天容量溢出 → 降级最低分 POI 为「待选」（不删除）
  - 标注降级原因（"时间不够" / "不顺路" / "当天关闭"）
  - 若全部 POI 已排完且容量有余 → 从「待选」池中恢复
```

#### 3.4.3 关键参数

| 参数 | 值 | 说明 |
|------|----|------|
| 紧凑型日容量 $T_{day}$ | 480 分钟 | 8h 纯游玩（不含用餐和通勤） |
| 休闲型日容量 | 360 分钟 | 6h，留白多 |
| 标准型日容量 | 420 分钟 | 7h 默认 |
| 午餐窗 | 11:30~13:00 | 插入美食 POI 的候选窗口 |
| 晚餐窗 | 17:30~19:00 | |
| 通勤惩罚系数 $\lambda$ | 0.01 | 通勤时间权重（相对评分） |
| 2-opt 迭代轮数 | 100 | 超过此轮停止优化 |

#### 3.4.4 无解场景的降级策略

| 场景 | 降级策略 |
|------|---------|
| 所有日程都无法塞下全部 POI | 按评分排序，保留前 N 个能塞下的，其余标注「待选」|
| 某些 POI 时间窗冲突（如两家店同时关门） | 只选评分更高的那一个 |
| 住宿位置导致通勤总时间超出日容量 | 提示用户调整住宿位置或游玩节奏 |
| 美食 POI 太多导致午餐窗冲突 | 每餐只保留评分最高的 1 个 |

### 3.5 阶段五：结果展示与调整

#### 3.5.1 行程展示（时间线视图）

```
┌─────────────────────────────────────────────────┐
│ 📅 第 1 天 | 2026-05-01（周六）| 4 个去处        │
├─────────────────────────────────────────────────┤
│ 🌅 上午                                         │
│  09:00  大理古城（住宿点出发，步行 5 分钟）        │
│  ├ 古城人民路逛吃         🕐 2h  💰 ¥30~80      │
│  └ 步行至下一站 15 分钟                         │
│                                                 │
│ ☀️ 中午                                         │
│  11:30 🍽️ 再回首凉鸡米线    🕐 40min 💰 ¥15    │
│  └ 打车至下一站 20 分钟                         │
│                                                 │
│ 🌤️ 下午                                         │
│  13:00  寂照庵（多肉花园+斋饭） 🕐 1.5h 💰 免费  │
│  └ 打车至下一站 25 分钟                         │
│                                                 │
│ 🌙 傍晚                                         │
│  17:00  龙龛码头看日落      🕐 1h   💰 免费     │
│  └ 步行回住宿 30 分钟                           │
├─────────────────────────────────────────────────┤
│ 💰 今日预估费用：¥45~95（不含住宿）               │
│ 🚶 步行 50min | 🚗 车程 45min                   │
└─────────────────────────────────────────────────┘
│
│ ⚠️ 未安排（原因）：
│   苍山（排队太久，寂照庵更出片且同方向）
│   双廊古镇（不顺路，建议第二天单独去）
```

#### 3.5.2 行程调整操作

| 操作 | 交互方式 | 系统响应 |
|------|---------|---------|
| 删除 POI | 点击 × 按钮 | 该处留空，时间顺延或建议替代 |
| 增加 POI | 点击「+ 添加去处」→ 弹出候选列表 | 调用增量重规划，调整后续时间 |
| 修改节奏 | 顶部切换「休闲/标准/紧凑」 | 全行程重规划 |
| 修改预算 | 顶部预算滑块 | 过滤超出预算的 POI，重规划 |
| 拖拽排序 | 同一天内拖拽 POI 位置 | 仅日内重排序 |
| 跨天移动 | 拖拽到另一天 | 两天的行程重规划 |
| 一键重规划 | 点击「重新规划」按钮 | 不保留手动调整，从零重新求解 |

#### 3.5.3 错误态处理

| 场景 | UI 展示 |
|------|--------|
| 规划超时（>30 秒） | "正在优化路线中…" 加载动画 + 「简化版」降级按钮 |
| 规划无解 | "当前约束太严格，试试这些调整：放宽节奏、减少必去、调整住宿" |
| POI 图片加载失败 | 灰色占位图 + POI 名称水印 |
| API 调用失败 | 顶部 toast 提示「网络异常，已使用离线数据」|

---

## 4 数据管道——离线数据建设

### 4.1 Bing 搜索摘要管道

**目标：** 为每个 POI 生成一条 40 字以内的特色摘要，基于多平台真实评价聚合。

```
Step 1: browser_use 搜索
        关键词："{POI名称} {城市} 评价 避坑 推荐"
        → 提取前 5 条搜索结果摘要
        → 自动去重（相似度 > 0.8 视为重复）
        → 标注来源平台（什么值得买/知乎/携程/马蜂窝/网易）

Step 2: LLM 提炼 (GPT-4o-mini)
        Prompt:
        "以下为关于「{POI名称}」的真实评价片段，来自多个旅游平台。
         提炼一条 40 字以内的特色摘要，要求：
         ① 语气轻松如朋友推荐，不用官方口吻
         ② 必须包含 1 个独特亮点
         ③ 必须包含 1 条实用信息（时间/费用/避坑）
         ④ 不编造不存在的信息。不确定的信息用「据说」「很多游客反馈」

         评价片段：
         {review_snippets}"
        → 输出：40 字摘要

Step 3: 质量过滤
        - 长度 15~50 字（超出范围重新生成）
        - 不含「必去」「绝美」「震撼」等空洞形容词（超过 2 个重新生成）
        - 不含编造数据（交叉验证原始评价片段）

Step 4: 存入 POI 表
        字段：summary, review_sources[], review_count_base,
              summary_updated_at, search_hotness
```

**管道参数：** 80 POI × 5 秒/条 = ~7 分钟/城市 | LLM token 成本 ~¥0.16/城市

**异常处理：**
- 搜索超时（>10s）→ 重试 1 次，仍失败则标注 `search_hotness=0, summary=NULL`
- LLM 提炼失败 → 直接用搜索摘要第一条（去重后）
- 搜索结果为 0 → 标记 POI 为「待补充」，summary 留空

### 4.2 Bing 图片管道

**目标：** 为每个 POI 采集 1 张封面 + 4 张画廊图，模拟游客实拍风格。

```
Step 1: 搜索封面图
        关键词："{POI名称} {城市} 风景 高清"
        → 选第 1 张符合质量标准的

Step 2: 搜索画廊图
        关键词："{POI名称} {城市} 实拍 游客 打卡"
        → 选前 4 张符合质量标准的

Step 3: 质量过滤（全自动）
        ✓ 最低分辨率：封面 ≥ 800×600，画廊 ≥ 400×300
        ✓ 自动拒绝：
          - 纯文字/logo/水印面积 > 20%
          - 纯白/纯黑/纯色背景
          - 文件大小 < 2KB（可能是占位图）
          - 宽高比异常（< 0.5 或 > 2.0）

Step 4: 下载 + WebP 压缩
        封面：800×600，quality=80 → ~50KB
        画廊：400×300，quality=75 → ~20KB/张
        → 存入 images/{city_id}/{poi_id}/

Step 5: 人工抽检
        每个城市随机抽 10 个 POI，人工过一眼封面
        不合格率 > 20% 则扩大抽检至 20 个
```

**管道参数：** 80 POI × 2 次搜索 + 下载 = ~5 分钟/城市 | 存储 ~10MB/城市

**异常处理：**
- 图片下载超时（>5s）→ 跳过该图片，标注 `gallery_missing_count`
- 无合格封面 → 使用默认占位图 + POI 名称水印
- 版权声明：在卡片左下角标注「图片来自公开搜索结果」，非商业用途

### 4.3 离线距离矩阵

**目标：** 预计算所有 POI 对之间的驾车/公交行程时间，消除规划时的 API 调用。

```
计算范围：C(80, 2) = 3,160 对路线
API：高德路径规划 API（驾车 + 公交）

Step 1: 批量请求（遵守 API 限流）
        - 每批 30 对，间隔 1 秒
        - 总耗时 ~53 分钟
        - 可中断续传（记录进度，重启后跳过已计算的）

Step 2: 存入 SQLite
        表结构：distance_matrix(from_poi_id, to_poi_id,
                  drive_minutes, transit_minutes, walk_meters)

Step 3: 增量更新
        新增 POI 时：计算新 POI 与所有已有 POI 的距离（N 次调用）
        删除 POI 时：标记相关记录为过期，不立即删除
```

**异常处理：**
- 高德 API 限流（QPS 超限）→ 自动降至每秒 20 次，加间隔
- 单对计算失败 → 重试 1 次，仍失败则标注 `drive_minutes=NULL`（规划引擎用直线距离 × 1.4 估算）

---

## 5 数据模型

### 5.1 City（城市）

| 字段 | 类型 | 约束 | 示例 |
|------|------|------|------|
| city_id | TEXT PK | 唯一 | "DL_001" |
| name | TEXT | NOT NULL | "大理" |
| province | TEXT | | "云南" |
| suggested_days | TEXT | JSON 数组 | "[2, 4]" |
| best_seasons | TEXT | JSON 数组 | "[3,4,5,9,10,11]" |
| center_lat | REAL | | 25.6065 |
| center_lon | REAL | | 100.2299 |
| districts | TEXT | JSON 数组 | "[\"古城\",\"双廊\",\"喜洲\",\"海东\"]" |
| poi_count | INTEGER | | 87 |
| status | TEXT | "active"/"building" | "active" |
| created_at | DATETIME | | "2026-05-14T00:00:00" |

### 5.2 POI（兴趣点）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| poi_id | TEXT PK | 唯一 | "DL_POI_042" |
| city_id | TEXT FK | → City | "DL_001" |
| name | TEXT | NOT NULL | "洱海生态廊道" |
| type | TEXT | 景点/美食/活动/购物 | "景点" |
| lat | REAL | | 25.6152 |
| lon | REAL | | 100.2123 |
| duration_min | INTEGER | 分钟 | 150 |
| open_time | TEXT | "HH:MM" | "06:00" |
| close_time | TEXT | "HH:MM" | "20:00" |
| avg_cost | INTEGER | 人均 ¥ | 75 |
| rating | REAL | 0~5 | 4.5 |
| rating_count | INTEGER | | 1280 |
| tags | TEXT | JSON 数组 | "[\"骑行\",\"日出\",\"拍照\"]" |
| summary | TEXT | LLM 提炼 40 字 | "环湖骑行很舒服..." |
| review_sources | TEXT | JSON 数组 | "[\"什么值得买\",\"知乎\"]" |
| review_count_base | INTEGER | | 127 |
| search_hotness | INTEGER | Bing 提及频次 | 8230 |
| cover_image | TEXT | 相对路径 | "images/DL_001/DL_POI_042/cover.webp" |
| gallery | TEXT | JSON 数组，4 张 | "[\"gal_01.webp\",...]" |
| image_source | TEXT | | "Bing图片搜索" |
| gallery_missing | INTEGER | 缺失图片数 | 0 |
| district | TEXT | 所属片区 | "古城" |
| need_booking | INTEGER | 0/1 | 0 |
| status | TEXT | "active"/"pending"/"removed" | "active" |
| created_at | DATETIME | | |
| summary_updated_at | DATETIME | 上次摘要更新 | |
| image_updated_at | DATETIME | 上次图片更新 | |

### 5.3 User（用户）

| 字段 | 类型 | 说明 |
|------|------|------|
| user_id | TEXT PK | UUID |
| nickname | TEXT | |
| created_at | DATETIME | |
| last_active_at | DATETIME | |

### 5.4 UserPreference（单次行程的约束快照）

| 字段 | 类型 | 说明 |
|------|------|------|
| preference_id | TEXT PK | UUID |
| user_id | TEXT FK | → User |
| city_id | TEXT FK | → City |
| num_people | INTEGER | 出行人数 |
| arrival_time | DATETIME | |
| departure_time | DATETIME | |
| accommodation_lat | REAL | |
| accommodation_lon | REAL | |
| transport_mode | TEXT | "bus"/"taxi"/"self_drive"/"bike"/"walk" |
| pace | TEXT | "relaxed"/"standard"/"compact" |
| food_preference | TEXT | "local"/"trendy"/"fine_dining"/"mixed" |
| budget_per_day | INTEGER | 人均 ¥ |
| special_requirements | TEXT | |
| created_at | DATETIME | |

### 5.5 POIScore（POI 评分记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| score_id | TEXT PK | UUID |
| preference_id | TEXT FK | → UserPreference |
| poi_id | TEXT FK | → POI |
| score | INTEGER | 1~5（来自手势或点击） |
| gesture | TEXT | "swipe_right"/"swipe_left"/"swipe_up"/"swipe_down"/"tap_star" |
| created_at | DATETIME | |

### 5.6 DailyRoute（每日行程方案）

| 字段 | 类型 | 说明 |
|------|------|------|
| route_id | TEXT PK | UUID |
| preference_id | TEXT FK | → UserPreference |
| day_number | INTEGER | 第几天 |
| date_label | TEXT | "2026-05-01（周六）" |
| poi_sequence | TEXT | JSON 数组："[{\"poi_id\":\"...\",\"arrival\":\"09:00\",\"departure\":\"11:00\",\"note\":\"\"}]" |
| total_cost | INTEGER | 当日费用 |
| total_walk | INTEGER | 步行分钟 |
| total_drive | INTEGER | 车程分钟 |
| created_at | DATETIME | |
| is_adjusted | INTEGER | 0=初版, 1=用户调整过 |

### 5.7 Session（会话状态）

| 字段 | 类型 | 说明 |
|------|------|------|
| session_id | TEXT PK | UUID |
| user_id | TEXT FK | → User |
| preference_id | TEXT FK | → UserPreference |
| current_phase | TEXT | "city_input"/"card_flow"/"constraint"/"planning"/"result"/"adjust" |
| created_at | DATETIME | |
| updated_at | DATETIME | |
| expires_at | DATETIME | 24 小时后过期 |

### 5.8 DistanceMatrix（离线距离矩阵）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | INTEGER PK | 自增 |
| from_poi_id | TEXT FK | → POI |
| to_poi_id | TEXT FK | → POI |
| drive_minutes | INTEGER | 驾车时间 |
| transit_minutes | INTEGER | 公交时间 |
| walk_meters | INTEGER | 步行距离 |
| created_at | DATETIME | |
| UNIQUE(from_poi_id, to_poi_id) | | |

---

## 6 前端设计

### 6.1 技术选型

| 组件 | 选型 | 理由 |
|------|------|------|
| 框架 | React 18 + Vite + TypeScript | 生态成熟，HMR 快速开发 |
| 卡片滑动 | react-tinder-card | Tinder 式手势滑动，支持 5 方向回调 |
| 图片画廊 | swiper.js (Swiper Element) | Web Component 原生，触摸左右滑动浏览 |
| 动效 | framer-motion | 卡片飞出/进入/星级动画 |
| 样式 | tailwindcss | utility-first，快速迭代 |
| 状态管理 | zustand | 轻量（< 2KB），TypeScript 友好 |
| 路由 | react-router v6 | SPA 页面间导航 |
| HTTP | fetch + React Query (TanStack) | 缓存、重试、加载态管理 |

### 6.2 API 接口定义

#### 通用约定

- Base URL: `/api/v1`
- Auth: Bearer Token (JWT)
- Content-Type: `application/json`
- 错误响应：`{"error": {"code": "CITY_NOT_FOUND", "message": "..."}}`

#### 端点列表

```
POST   /api/v1/session                    → 创建/恢复会话
  Request:  {"user_id": "...", "city_name": "大理", "days": 3}
  Response: {"session_id": "...", "phase": "card_flow", "city": {...}}

GET    /api/v1/city/{city_name}            → 城市信息
  Response: {"city_id": "...", "name": "大理", "poi_count": 87, ...}

GET    /api/v1/poi/cards?session_id=...    → 下一批 3 张卡片
  Response: {"cards": [{...}, {...}, {...}], "remaining": 24}

POST   /api/v1/poi/{poi_id}/score         → 保存评分
  Request:  {"session_id": "...", "score": 5, "gesture": "swipe_right"}
  Response: {"ok": true}

POST   /api/v1/poi/{poi_id}/detail        → POI 详情（全画廊+完整摘要）
  Request:  {"session_id": "..."}
  Response: {"poi": {...}, "gallery": ["url1", "url2", ...]}

GET    /api/v1/profile/{session_id}       → 当前约束快照
  Response: {"preference_id": "...", "num_people": null, ...}

POST   /api/v1/profile/{session_id}       → 更新约束
  Request:  {"num_people": 2, "pace": "relaxed", ...}
  Response: {"ok": true, "inferred": {"节奏已从默认改为休闲型"}}

POST   /api/v1/planner/run                → 运行规划引擎
  Request:  {"session_id": "..."}
  Response: {"routes": [Day1, Day2, Day3], "excluded": [...]}

POST   /api/v1/planner/adjust             → 调整行程
  Request:  {"session_id": "...", "action": "add", "poi_id": "..."}
           {"session_id": "...", "action": "remove", "poi_id": "..."}
           {"session_id": "...", "action": "change_pace", "pace": "compact"}
  Response: {"routes": [...], "changes": ["已将苍山替换为寂照庵"]}

GET    /api/v1/planner/result/{session_id} → 获取已生成的行程
  Response: {"routes": [...], "excluded": [...]}
```

### 6.3 前端状态机

```
┌──────────┐   输入城市    ┌───────────┐   评分≥退出阈值   ┌───────────┐
│ CITY_INPUT│────────────→│ CARD_FLOW │────────────────→│CONSTRAINT │
└──────────┘              └───────────┘                  └─────┬─────┘
                                                               │ 约束收集完成
                                                               ▼
┌──────────┐   用户调整    ┌──────────┐    规划完成    ┌───────────┐
│  ADJUST  │←────────────│  RESULT  │←──────────────│ PLANNING  │
└──────────┘              └──────────┘               └───────────┘
     │ 重规划                  │                           ↑
     └────────────────────────┘                     规划完成时
```

### 6.4 UI 状态覆盖

每个页面/组件需覆盖以下状态：

| 状态 | 处理方式 |
|------|---------|
| **Loading** | Skeleton 占位（卡片灰色块、文字闪烁块） |
| **Empty** | 友好提示 + 操作引导（"这个城市还没有吃喝玩乐数据，试试别的？"） |
| **Error** | Toast 通知 + 重试按钮（"网络不太稳，点这里重试" → 调用上次失败请求） |
| **Success** | 正常渲染 |
| **Offline** | 显示离线可用数据 + "联网可获得实时信息" 提示 |

---

## 7 后端 Agent 设计

### 7.1 Agent 架构

**主控引擎：** LangChain ReAct Agent

**状态机：**
```
INIT → CITY_INPUT → CARD_FLOW → CONSTRAINT → PLANNING → RESULT → ADJUST
  ↑                                                                │
  └──────────────────── 一键重规划 ─────────────────────────────────┘
```

#### System Prompt 模板

```yaml
role: 旅行规划师
identity: 友好、专业、有主见的旅行助手。用口语化表达推荐，用结构化数据排程。
name: "Jarvis"（内部标识）

behavior_rules:
  - 对话风格：像朋友推荐而非客服回答，用"~"结尾增加亲和力
  - 信息可信度：所有 POI 信息来自高德和 Bing 搜索，不会编造
  - 推荐偏好：优先推荐评分高且用户已右滑的 POI
  - 时间意识：所有时间建议基于距离矩阵的真实数据
  - 纠错能力：用户说"不对，我不想去那个"时立即调整

tool_usage:
  - get_city_info: 查询城市概况
  - get_poi_cards: 获取下一批卡片（每次 3 张）
  - get_poi_detail: 获取 POI 完整详情
  - save_poi_score: 保存用户对 POI 的评分
  - get_profile: 获取当前约束快照
  - update_profile: 更新约束（含智能推断）
  - run_planner: 运行规划引擎
  - adjust_planner: 调整已有行程
  - amap_geocode: 高德地理编码（地址→经纬度）

error_recovery:
  - 工具调用失败 → 重试 1 次，仍失败则告知用户并提供替代方案
  - LLM 输出不符合 schema → 要求 LLM 重新输出，最多 2 次
```

#### Agent 决策权限边界

| 可以自主决定 | 需要用户确认 |
|-------------|-------------|
| POI 推荐排序 | 删除用户已标记"想去"的 POI |
| 约束推断（"我和女朋友"→2人） | 修改用户明确输入的约束 |
| 默认参数选择（休闲型） | 放弃高评分 POI（需标注原因，用户可拒绝） |
| 行程时间计算 | 大幅改变行程结构（>50% POI 被替换） |
| 替代方案推荐 | 超出预算范围的 POI |

### 7.2 工具函数定义

```python
# 资源获取
get_city_info(city_name: str) -> City
get_poi_cards(session_id: str, count: int = 3) -> list[POICard]
get_poi_detail(session_id: str, poi_id: str) -> POIFull

# 用户状态
save_poi_score(session_id: str, poi_id: str, score: int, gesture: str) -> None
get_profile(session_id: str) -> UserPreference
update_profile(session_id: str, updates: dict) -> UserPreference

# 规划引擎
run_planner(session_id: str) -> PlannerResult
adjust_planner(session_id: str, action: str, **kwargs) -> PlannerResult

# 外部服务
amap_geocode(address: str) -> tuple[float, float]  # (lat, lon)

# 离线管道（开发者工具，非 Agent 运行时调用）
bing_summary_enrich(city_id: str) -> None       # 摘要管道
bing_image_collect(city_id: str) -> None         # 图片管道
build_distance_matrix(city_id: str) -> None      # 距离矩阵
```

### 7.3 离线数据管理脚本

| 脚本 | 功能 | 执行顺序 | 预计耗时 |
|------|------|---------|---------|
| `pipeline_amap_fetch.py` | 高德 POI 采集 → poi_cache.db | ① | ~10 分钟 |
| `pipeline_bing_summary.py` | Bing 搜索 → LLM 摘要 → 更新 poi_cache.db | ② | ~7 分钟 |
| `pipeline_bing_images.py` | Bing 图片采集 → images/ | ③ | ~5 分钟 |
| `pipeline_distance_matrix.py` | 全 POI 对距离 → distance.db | ④（可后台） | ~53 分钟 |

### 7.4 幻觉防范策略

| 风险 | 防范方法 |
|------|---------|
| Agent 编造 POI 名称 | 所有 POI 来自高德和 Bing 搜索管道，Agent 只能从 POI 表中读取，不允许生成 |
| Agent 编造评分/价格 | 强制绑定 POI 表字段，非数据库字段不下发 |
| Agent 编造路线时间 | 路线时间来自距离矩阵，无数据时标注"预估" |
| Agent 推荐不存在的餐厅 | get_poi_cards 只返回 status="active" 的 POI |
| 摘要 LLM 编造评价 | Prompt 明确"不编造不存在的信息"，交叉验证原始片段 |

---

## 8 非功能性需求

### 8.1 性能

| 项目 | 指标 | 测量方式 |
|------|------|---------|
| 卡片流响应 | ≤ 100ms | 后端 API p99 |
| 规划引擎 | ≤ 10 秒 | run_planner 函数 wall time |
| 首屏加载 | ≤ 2 秒 (LCP) | Lighthouse |
| 图片总大小 | ≤ 10MB/城市 | `du -sh images/` |
| 离线管道 | ≤ 30 分钟/城市 | Phase 1~3 wall time |

### 8.2 安全

| 项目 | 方案 |
|------|------|
| 身份认证 | JWT Token，24 小时过期 |
| 数据隔离 | 每个 Session 只能访问自己的评分和约束 |
| 输入校验 | 所有用户输入经过 FastAPI Pydantic 校验 |
| SQL 注入防护 | 全部使用参数化查询 |
| CORS | 仅允许前端域名 |

### 8.3 可访问性 (a11y)

| 项目 | 方案 |
|------|------|
| 键盘操作 | 卡片滑动支持 ← → 键；Tab 键遍历所有可操作元素 |
| 色盲友好 | 不去/想去不仅靠颜色区分（红/绿），还附带文字标签 |
| 屏幕阅读器 | 卡片内容有 aria-label；图片有 alt 文本 |

### 8.4 兼容性

| 项目 | 范围 |
|------|------|
| 浏览器 | Chrome 90+, Edge 90+, Firefox 90+, Safari 15+ |
| 屏幕分辨率 | ≥ 1280×720 |

### 8.5 可观测性

| 项目 | 方案 |
|------|------|
| Agent 决策日志 | 每次 Tool Call 的输入/输出记录到日志文件 |
| 错误追踪 | 所有异常捕获并记录，含 stack trace 和 session_id |
| 性能打点 | 关键路径（卡片请求、规划求解）记录 p50/p99 |

### 8.6 数据备份

- SQLite 文件每日备份到 `D:\X_PLAN\backups\`，保留最近 7 天
- 离线管道产出文件（距离矩阵、图片）纳入备份

---

## 9 POI 知识库构建策略

### 9.1 首批城市

| 城市 | POI 数量 | 优先级 | 目标状态 |
|------|---------|--------|---------|
| 大理 | 80~120 | P0 MVP | 完整三级管道 |
| 成都 | 80~120 | P1 | 完整三级管道 |
| 杭州 | 80~120 | P1 | 完整三级管道 |

### 9.2 四层数据来源

```
层级 1: 高德 POI API
        └→ 基础信息（名称、坐标、评分、营业时间、标签）

层级 2: Bing 搜索摘要管道
        └→ 多平台真实评价聚合 → LLM 提炼 40 字摘要 → 标注来源

层级 3: Bing 图片管道
        └→ 封面风光照 + 游客实拍画廊 → WebP 压缩

层级 4: 高德离线距离矩阵
        └→ 全 POI 对驾车/公交时间 → 规划引擎 0 次实时调用
```

### 9.3 POI 筛选标准

入选条件（需同时满足 ≥ 3 条）：

1. 高德评分 ≥ 3.8
2. 高德评价数 ≥ 100 或 Bing 搜索提及频次 ≥ 500
3. 有明确的营业时间信息
4. 游客友好（非纯本地人去的封闭场所，如机关单位、小区内部）

### 9.4 POI 数据维护策略

| 场景 | 处理方式 |
|------|---------|
| POI 关门/歇业 | 用户反馈 + 定期复核 → 标记 `status="removed"` |
| 新开业 POI | 定期（每月）跑高德采集脚本，对比新增 |
| 季节性变化 | 营业时间、最佳季节字段支持手动修正 |
| 摘要/图片过期 | TTL=30 天后自动触发全量刷新管道 |

---

## 10 Agent 行为规约与边界

### 10.1 反例清单（Agent 绝对不能做什么）

| ❌ 禁止行为 | 场景示例 |
|------------|---------|
| 编造 POI 信息 | ~~"洱海旁边有个网红悬崖咖啡馆"~~ → 数据库中无此 POI，不应推荐 |
| 推荐 status="removed" 的 POI | ~~推荐已关门餐厅~~ |
| 隐瞒规划局限性 | ~~说"这是最优路线"~~ → 应该说"这是我根据你的评分排出的路线，苍山因为时间不够没有安排" |
| 越权修改用户明确输入的约束 | ~~用户说"3天"→Agent 自行改为"4天"~~ |
| 推荐超出预算且无替代方案 | ~~推荐 ¥500 餐厅但对预算 ¥100 用户没有降级选项~~ |
| 忽略用户显式否定 | 用户说"不去苍山"后仍在后续推荐中提及 |

### 10.2 Agent 自主决策范围

| ✅ 可以自主 |
|------------|
| 根据评分排序推荐 POI |
| 推断隐含约束（"带爸妈"→休闲节奏） |
| 选择规划算法参数（日容量、午餐窗） |
| 推荐替代方案 |
| 用结构化数据（距离矩阵）计算时间 |

| ⚠️ 需要确认 |
|------------|
| 放弃用户右滑过的 POI（需标注原因） |
| 大幅改变行程（>50% POI 替换） |
| 突破预算约束 |

| 🚫 无权操作 |
|------------|
| 修改用户显式输入的约束值 |
| 删除用户已标记的评分数据 |
| 访问其他用户的 Session 数据 |

---

## 11 评估方案与质量保障

### 11.1 离线评估

**评估集构建：** 为大理构建 10 个标准测试场景，覆盖：

| 场景类型 | 示例 | 评估指标 |
|---------|------|---------|
| 短途紧凑 | 2 天 6 个想去 | 时间窗违规率、评分利用率 |
| 长线休闲 | 5 天 3 个想去 | 日容量填充率、闲散感 |
| 预算硬约束 | 人均 ¥100/天 | 预算违规率 |
| 美食导向 | 75% 评分给美食 POI | 美食 POI 占比、用餐时间合理性 |
| 极端通勤 | 住宿在双廊 | 通勤时间合理性、POI 放弃率 |

**评估方式：** 跑 `run_planner`，对比人工标注的"合理路线"。关键指标：
- 时间窗违规率 < 5%
- 评分利用率（高评分 POI 被选中的比例）> 70%
- 通勤时间占比 < 30%

### 11.2 失败模式监控

| 失败模式 | 监控方式 | 阈值 |
|---------|---------|------|
| 规划无解 | `run_planner` 返回空结果 | > 10% 触发告警 |
| 规划超时 | > 30 秒 | > 5% 触发告警 |
| Agent LLM 推理失败 | Tool Call 返回 error | > 3% 触发告警 |
| POI 摘要为空 | summary IS NULL 比例 | > 20% 触发管道重跑 |
| 图片空缺 | gallery_missing > 3 | 触发该 POI 图片补采 |

---

## 12 上线策略与演进路线

### 12.1 冷启动方案

```
第 1 步：大理 POI 管道全量跑通（~22 分钟）
第 2 步：距离矩阵后台构建（~53 分钟，不阻塞）
第 3 步：前后端本地联调通过
第 4 步：自己 + 2 个朋友内测（收集真实反馈）
第 5 步：修复关键问题后 → 公开可用
```

### 12.2 灰度节奏

```
内测（Week 1~2）：本人 + 2 人，核心流程走通
小范围（Week 3）：5~10 人，加入监控和日志
全量（Week 4）：公开可用，收集使用数据
```

### 12.3 V1 → V2 触发条件

| 触发条件 | V2 做什么 |
|---------|----------|
| 大理城市完成度 100%（全部 POI 有摘要+图片+距离矩阵） | 扩展成都 |
| 首版行程满意度 > 70%（未重规划率） | 保持现状 |
| 首版行程满意度 < 50%（频繁重规划） | 改进推荐算法或规划引擎 |
| 用户反馈"想规划多城市" > 3 次 | 启动多城市规划方案设计 |

### 12.4 数据飞轮设计

```
用户评分 → 聚合偏好模式（哪些类型 POI 最常被右滑）
         → 调整推荐策略权重
         → 个性化推荐更精准

用户重规划行为 → 分析被删除/替换的 POI
              → 优化规划引擎参数
              → 首版更贴合用户预期

用户不使用行为 → 哪些 POI 从未被评分
              → 检查摘要/图片/评分质量
              → 触发数据刷新
```

### 12.5 回滚条件

| 条件 | 动作 |
|------|------|
| 规划引擎错误率 > 20% | 回退到上一个稳定版本 |
| 前端严重 bug（白屏/卡死） | 回退前端版本，修复后重新部署 |
| 数据管道产出脏数据 | 恢复上一次备份的 poi_cache.db |

---

## 13 风险与应对

### 13.1 Agent 特有风险

| 风险 | 影响 | 概率 | 应对 |
|------|------|------|------|
| **LLM 幻觉** | 推荐不存在的 POI 或编造信息 | 中 | 强制绑定数据库字段，禁止自由生成 POI |
| **上下文溢出** | 对话过长导致 token 超限 | 中 | 上下文压缩（只保留关键约束 + 最近 3 轮对话） |
| **Token 成本失控** | 单次规划调用过多 LLM | 低 | 预算 ≤5000 tokens/次，超限降级为规则引擎 |
| **工具调用死循环** | Agent 反复调用同一工具 | 低 | 单工具调用上限 3 次/阶段 |
| **数据泄露** | Agent 访问其他用户 Session | 极低 | SQLite 隔离 + Session ID 校验 |
| **规划质量下降** | 距离矩阵过期，通勤时间不准 | 中 | TTL=30 天自动刷新 + 降级为直线估算 |

### 13.2 通用风险

| 风险 | 影响 | 应对 |
|------|------|------|
| 高德 API 限流 | 管道/距离矩阵构建受阻 | 自动降速 + 中断续传 |
| Bing 搜索不稳定 | 摘要/图片管道产率低 | 质量过滤放宽 + 人工补充 |
| 用户期望过高 | 对路线"不满意" | 明确标识系统局限性 + 提供人工调整入口 |
| 个人学习项目缺乏维护 | 长期无人更新 | 代码开源 + 文档完善 + 零外部依赖 |

---

## 14 开发路线图

### Phase 1：大理单城 MVP（预估 40~60 小时）

| 任务 | 预估工时 | 完成标准 |
|------|---------|---------|
| 大理 POI 高德采集（80+ 条） | 4h | poi_cache.db 填充 |
| Bing 摘要管道 | 6h | 80 条摘要入库，成功率 > 80% |
| Bing 图片管道 | 4h | 封面+画廊入库，缺失率 < 10% |
| 离线距离矩阵 | 4h | distance.db 填充 ≥ 3000 条 |
| 后端 FastAPI + 路由 | 8h | 8 个 API 端点全部可调通 |
| LangChain Agent 集成 | 8h | 状态机全流程走通 |
| 前端 React + TinderCard | 12h | 卡片滑动+评分交互完成 |
| 约束收集 UI | 4h | 对话式表单完成 |
| 规划引擎 | 6h | 贪心+2-opt 产出合理路线 |
| 行程展示 | 4h | 时间线视图完成 |

**MVP 验收场景：** "用户打开页面 → 输入大理 3 天 → 刷 10 张卡片 → 回答约束 → 10 秒内获得 3 天行程 → 可以删除/增加 POI → 重规划完成"

### Phase 2：体验优化（预估 20~30 小时）

- [ ] 图片画廊滑动打磨（swiper 动效 + 懒加载）
- [ ] 卡片动效（framer-motion 飞出/进入动画）
- [ ] 行程交互调整（拖拽排序、跨天移动）
- [ ] 异常状态全覆盖（Loading/Empty/Error/Offline）
- [ ] 键盘操作 + 色盲友好
- [ ] 移动端响应式适配（虽然 v1 重点是 PC）

### Phase 3：扩展（预估 30~40 小时）

- [ ] 成都 POI 库 + 完整管道
- [ ] 杭州 POI 库 + 完整管道
- [ ] 多交通方式对比（驾车 vs 公交 vs 骑行）
- [ ] 预算硬约束（超出即排除）
- [ ] 行程导出（Markdown/图片分享）

---

## 15 关键决策记录

| 日期 | 决策 | 理由 |
|------|------|------|
| 05-03 | 规划引擎用贪心+2-opt 而非 MIP 精确解 | MIP 求解 80 POI 需数分钟，启发式 ≤10 秒够用 |
| 05-03 | v1 仅单城市 | 多城市需城际交通模型，v2 再引入 |
| 05-14 | 评价数据→Bing 搜索管道 | 小红书无 API 且反爬严重，Bing 可合法聚合多平台 |
| 05-14 | 离线距离矩阵（非实时） | 消除高德 API 实时调用，用户端 0 秒等待 |
| 05-14 | 前端换 React+TinderCard（非 Streamlit） | Streamlit 无法实现 Tinder 式滑动交互 |
| 05-14 | POI 图片→Bing 图片管道 | 封面+实拍画廊，比纯占位图真实感强 |
| 05-14 | v6 新增：Agent 行为规约 + 反例清单 | prd-review-panel 评审发现原 PRD 缺乏 Agent 边界定义 |

---

## 16 技术选型总表

| 组件 | 选型 | 备注 |
|------|------|------|
| 前端框架 | React 18 + Vite + TypeScript | SPA，首屏 < 200KB |
| 卡片交互 | react-tinder-card | 5 方向手势支持 |
| 图片画廊 | swiper.js (Swiper Element) | Web Component 原生 |
| 动效 | framer-motion | 卡片飞出/星级动画 |
| 样式 | tailwindcss | utility-first |
| 状态管理 | zustand | < 2KB |
| 数据请求 | TanStack React Query | 缓存/重试/加载态 |
| 后端框架 | FastAPI (Python 3.12) | 异步高性能 |
| Agent 框架 | LangChain ReAct | 工具调用 + 状态机 |
| LLM | GPT-4o-mini | 成本 ¥0.16/城市（摘要）+ ~¥0.5/次规划 |
| 数据库 | SQLite（3 个文件） | 零配置，本地部署 |
| 地图 | 高德 API | POI 搜索 + 路径规划 + 地理编码 |
| 搜索管道 | browser_use + Bing | 评价 + 图片双管道 |
| 规划求解 | 自研贪心 + 2-opt | Python 实现，不依赖 ortools |
| 部署 | 本地运行（localhost） | v1 非商用，无服务器成本 |
